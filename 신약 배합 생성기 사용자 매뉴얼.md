
---
## 1. 프로그램 소개

신약 배합 생성기는 사용자가 자연어로 입력한 제제 조건을 분석하여 경구 고형제의 조성 후보를 생성하는 연구 보조 프로그램입니다.

프로그램은 로컬 데이터베이스의 부형제 정보와 HPE6 근거를 조회하고, 생성된 조성을 6개 검증 게이트로 확인합니다.

### 지원 제형

- 속방정
- 필름코팅정
- 장용정
- 서방정 및 방출조절정
- 구강붕해정
- 저작정
- 경질캡슐
- 경구 과립
- 경구 펠렛
- 경구 산제

> 본 프로그램은 연구 검토용 시제품입니다. 생성된 조성은 확정 처방이 아니며, 실제 사용 전에 연구원의 검토가 필요합니다.

---

## 2. 실행 프로그램

프로그램을 실행하기 전에 다음 항목을 준비합니다.

github releases 주소: https://github.com/ldr77a/PhramaProto-0.1/releases/tag/v0.1.0

- 위의 주소에서 "PhramaProto-0.1-Windows-x64.zip" 파일 다운
- zip파일 압축 해제 후 "start.bat" 파일 실행
- OpenAI, Gemini 또는 Claude API 키
- OpenAI, Claude API 키는 선불 결제 필요

첫 실행에는 Python과 필수 프로그램을 자동으로 내려받기 때문에 Python을 사용자가 별도로 설치할 필요는 없습니다.

---

## 3. 자동 설치 대기

첫 실행에서는 다음 항목이 자동으로 설치됩니다.

- 검증된 `uv` 실행 도구
- Python 3.12.13 (path: C:\Users\<user>\AppData\Local\PhramaProto\runtime\python)
- 프로그램 실행에 필요한 Python 패키지

설치가 끝날 때까지 검은 창을 닫지 않습니다. 첫 실행은 인터넷 속도에 따라 이후 실행보다 오래 걸릴 수 있습니다.

> 사용자가 Python 설치 프로그램을 별도로 실행하거나 설치 경로를 선택할 필요는 없습니다.

### 3.1 웹 화면 확인

설치와 실행이 완료되면 기본 브라우저에서 프로그램 화면이 자동으로 열립니다.

`127.0.0.1`은 사용자 컴퓨터 내부에서만 접근하는 로컬 주소입니다. 포트 번호는 실행할 때마다 달라질 수 있습니다.

---

## 4. 데이터베이스 상태 확인

메인 화면 상단의 `데이터베이스` 상태를 확인합니다.

정상적으로 실행되었다면 다음과 비슷하게 표시됩니다.

`ok · 앱 0.1.0 · DB [Snapshot ID] / schema [버전]`

---

## 5. AI 모델 선택

"모델 종류" 항목에서 사용할 AI 서비스를 선택합니다.

선택 가능한 공급자는 다음과 같습니다.

- OpenAI
- Gemini
- Claude

보유하고 있는 API 키와 동일한 공급자를 선택해야 합니다.

예를 들어 Gemini API 키를 보유하고 있다면 공급자도 `Gemini`로 선택해야 합니다. 공급자와 API 키가 일치하지 않으면 인증 오류가 발생합니다.

OpenAI, Claude는 선불 결제 형태입니다. 

---

## 6. 모델 등급 선택

모델 등급에서 작업 목적에 맞는 품질 수준을 선택합니다.

![[Pasted image 20260820001924.png|411]]

위에서 차례로 성능이 좋은 모델입니다.

처음 사용하는 경우에는 `보통`을 권장합니다.

모델 등급이 높을수록 일반적으로 분석 품질이 향상될 수 있지만, 호출 비용과 응답 시간이 증가할 수 있습니다. 실제 비용은 선택한 AI 공급자의 요금 정책에 따라 달라집니다.

---

## 7. API 키 적용

### 8.1 API 키 입력

`이번 실행의 API 키` 입력란에 선택한 공급자의 API 키를 입력합니다.

### 7.1 API 키 적용

API 키를 입력한 후 `이번 실행에 적용` 버튼을 클릭합니다.

정상적으로 적용되면 다음 메시지가 표시됩니다.

`이번 실행에 API 키를 적용했습니다.`

### API 키 보관 방식

- API 키는 현재 실행 중인 프로그램의 메모리에만 저장됩니다.
- 프로그램을 종료하면 API 키가 삭제됩니다.
- 프로그램을 다시 실행하면 API 키를 다시 입력해야 합니다.
- 입력한 API 키는 SQLite 데이터베이스에 저장되지 않습니다.

---

## 8. 연구 질문 입력

`연구 질문` 입력란에 만들고 싶은 제제 조건을 자연어로 입력합니다.

![[Pasted image 20260820002218.png]]

### 8.1 최소 입력 정보

질문에는 다음 세 가지 정보를 포함하는 것이 좋습니다.

1. 주성분명
2. 주성분 함량
3. 제형

예시:

> 아세트아미노펜 500 mg 속방정제를 제조해라.

### 8.2 제조 방법 추가

제조 방법을 알고 있다면 질문에 함께 입력합니다.

예시:

> 아세트아미노펜 500 mg 속방정제를 직접타정법으로 제조해라.

### 8.3 상세 조건 추가

필요한 경우 다음 조건도 지정할 수 있습니다.

- 목표 총중량
- 후보 개수
- 방출 특성
- 제조 공정
- 사용할 부형제
- 필요한 부형제 기능

상세 입력 예시:

> 아세트아미노펜 500 mg 속방정제를 직접타정법으로 제조하고, 총중량 650 mg 기준으로 조성 후보 3개를 제시해라.

서방정 예시:

> 메트포르민염산염 500 mg 서방정을 제조하고, 총중량 800 mg 기준으로 방출조절제를 포함한 조성 후보 3개를 제시해라.

경질캡슐 예시:

> 오메프라졸 20 mg 경질캡슐의 조성 후보 3개를 생성해라.

> 성분명, 함량, 제형을 명확하게 입력할수록 질문 해석 정확도가 높아집니다.

---

## 9. 조성표 생성

질문 입력이 끝나면 `조성표 생성` 버튼을 클릭합니다.`

AI 공급자 응답과 데이터베이스 조회가 완료될 때까지 기다립니다. 처리 중에는 버튼을 반복해서 누르지 않습니다.

---

## 10. 생성 결과 확인

생성이 완료되면 하나 이상의 조성 후보가 표시됩니다.

각 후보는 다음 세 가지 상태 중 하나로 표시됩니다.

### 11.1 배합 가능

`✅ 배합 가능`

기본 검증 게이트를 통과한 후보입니다.

### 11.2 조건부 후보

`⚠️ 조건부 후보`

사용할 수 있는 가능성은 있지만 주의사항이나 확인할 항목이 존재하는 후보입니다.

### 11.3 미해결

`❌ 미해결`

필수 기능, 총중량, 함량 또는 호환성 문제 등이 해결되지 않은 후보입니다. 

---

## 11. 조성표 읽는 방법

각 조성 후보에는 다음 정보가 표시됩니다.

![[Pasted image 20260820002408.png|642]]

|항목|의미|
|---|---|
|성분|주성분 또는 부형제 이름|
|기능|API, 희석제, 결합제, 붕해제, 활택제 등|
|mg|정제 또는 단위 제형당 함량|
|%|전체 조성에서 차지하는 비율|
|근거|해당 함량 또는 성분 선택에 사용한 자료|
|합계|전체 성분의 총중량과 총 비율|

조성표 하단의 합계가 목표 총중량 및 `100%`와 일치하는지 확인합니다.

---

## 12. HPE6 근거 확인

조성 후보 아래의 `HPE6 근거 보기`를 클릭하면 각 부형제의 상세 근거를 확인할 수 있습니다.

![[Pasted image 20260820002355.png|568]]

근거 영역에는 다음 정보가 표시될 수 있습니다.

- 모노그래프명과 PDF 페이지
- 용도별 권장 사용 범위
- 부적합성 대상
- 안정성 정보
- 주요 물성 정보

`HPE6 직접 근거 없음`이 표시되는 성분은 현재 데이터베이스에서 직접 연결된 HPE6 근거를 찾지 못한 경우입니다. 이때는 다른 근거와 원문 자료를 추가로 확인해야 합니다.

---

## 13. 게이트 검증 결과 확인

각 후보 하단에는 5개 게이트의 검증 결과가 표시됩니다.

각 게이트의 정의는 왼쪽 토글을 누르면 확인 가능합니다.
![[Pasted image 20260820002457.png]]

게이트 결과는 후보의 다음 조건을 자동으로 확인하는 데 사용됩니다.

- 사용량 범위
- 성분 간 호환성
- 제형에 필요한 기능 충족
- 제조 가능성
- 총중량 및 함량 조건
- 전체 조성 합계

경고 또는 실패가 있는 경우 후보 하단에 이유가 표시됩니다.

> `배합 가능`으로 표시되더라도 실제 제조 가능성, 원료 등급, 공정 조건, 안정성 및 시험 결과는 연구원이 별도로 검토해야 합니다.

---

## 14. 다른 질문 실행

새로운 질문을 실행하려면 기존 연구 질문을 삭제하고 새로운 조건을 입력합니다.

동일한 모델 종류류를 사용하는 경우 현재 실행 중에는 API 키를 다시 입력할 필요가 없습니다.

다른 공급자로 변경한다면 해당 모델 종류의 API 키를 새로 입력하고 `이번 실행에 적용`을 눌러야 합니다.

---

## 15. 프로그램 종료

브라우저 탭만 닫아도 검은 실행 창이 남아 있으면 프로그램이 계속 실행 중일 수 있습니다.

프로그램을 완전히 종료하려면 다음 중 하나를 수행합니다.

- 검은 실행 창에서 `Ctrl+C` 입력
- 검은 실행 창 닫기
- 프로그램이 실행된 명령 프롬프트 종료

프로그램을 종료하면 메모리에 보관된 API 키도 삭제됩니다.

---

## 16. 문제 해결

### 프로그램 화면이 열리지 않는 경우

1. 인터넷 연결을 확인합니다.
2. 검은 실행 창을 닫습니다.
3. `start.bat`을 다시 실행합니다.
4. 같은 오류가 반복되면 로그를 확인합니다.

로그 위치:

`%LOCALAPPDATA%\PhramaProto\logs\bootstrap.log`

### 주요 오류코드

|오류코드|의미|해결 방법|
|---|---|---|
|`APP-START-001`|설치 또는 실행 실패|인터넷을 확인하고 다시 실행|
|`LLM-KEY-001`|API 키 미설정|API 키 입력 후 적용|
|`LLM-AUTH-001`|API 키 인증 실패|공급자와 API 키 확인|
|`LLM-RATE-001`|API 호출 한도 초과|잠시 후 다시 실행하거나 계정 한도 확인|
|`LLM-TIMEOUT-001`|AI 응답 시간 초과|동일 질문으로 다시 시도|
|`LLM-UPSTREAM-001`|AI 공급자 연결 장애|네트워크 또는 공급자 상태 확인|
|`LLM-RESPONSE-001`|AI 응답 형식 오류|질문을 단순하게 수정하여 재시도|
|`DB-INTEGRITY-001`|데이터베이스 손상 가능성|새 ZIP을 다시 압축 해제|
|`DB-VERSION-001`|앱과 DB 버전 불일치|동일한 배포 버전 사용|
